# Dependencias da fase extra (Visao Computacional / CNN) - PyTorch.
# PyTorch usa a GPU NVIDIA direto no Windows nativo (sem WSL2).
#
# INSTALACAO (Windows, com GPU CUDA) - rode primeiro o torch pelo indice CUDA:
#     pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
# depois:
#     pip install -r requirements-cnn.txt
#
# Sem GPU (CPU) - o torch normal do PyPI ja serve:
#     pip install torch torchvision
#
# Confira a versao de CUDA recomendada em https://pytorch.org/get-started/locally/
pandas>=2.0
numpy>=1.24
matplotlib>=3.7
scikit-learn>=1.3
pillow>=10.0
scikit-image>=0.22
scipy>=1.10
